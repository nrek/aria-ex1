# Decisions Backlog

Cross-project architectural decisions captured during Claude Code sessions. Reviewed during the knowledge audit and either promoted to full ADRs in `decisions/` or cleared.

## Format

Each entry includes the date, project(s), decision context, and the decision itself. Optionally promoted to a full ADR immediately if context is fresh and the decision is significant.

---

(No pending decisions)
